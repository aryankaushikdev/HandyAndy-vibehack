{
  "name": "thumb-coach",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Live form + progress coach for the thumb-opposition (Kapandji) rehab exercise",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
