{
  "name": "movemend-gesture-coach",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "MoveMend Thumb Coach — live MediaPipe hand-tracking exercise coach. Runs as a companion app alongside the MoveMend dashboard.",
  "scripts": {
    "dev": "vite --port 5174",
    "build": "vite build",
    "preview": "vite preview --host --port 5174"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.18"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
